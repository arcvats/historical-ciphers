"use strict";
(() => {
  angular
    .module("app")
    .factory("STATIC", [
      "$http",
      $http => {
        return {};
      }
    ])
    .factory("API", [
      "$http",
      $http => {
        return {};
      }
    ]);
})();
